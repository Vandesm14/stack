"std/list.stack" import

("the" "words" "should" "be" "in" "order")
'() 'new-list set

'(fn
  ;; "Item: "
  ;; explode
  ;; swap

  ;; insert

  ;; "" join
  ;; print

  new-list
  swap
  insert

  'new-list set
)
list/for_each

new-list " " join
